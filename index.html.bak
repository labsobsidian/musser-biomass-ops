<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Lumber Buddy — Musser Biomass</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --black:#1A1410; --black-soft:#2E241D; --brown-dark:#3D2F25;
    --brown-mid:#6B5440; --brown-light:#9A826A; --border:#E4DED5;
    --surface:#F5F0E8; --bg:#FAF7F1; --white:#FFFFFF;
    --red:#B5392A; --red-light:#FBEDEA;
    --teal:#1E7F61; --teal-light:#E4F5EE;
    --amber:#C47A0A; --amber-light:#FCEFDA;
    --blue-light:#EBF1F8; --blue-text:#1F4E79;
    --success:#1E7F4A; --success-bg:#E4F4EC;
    --gold:#B8882B; --gold-light:#FAF0D6;

    /* Legacy var aliases for code that originally used Atlas gray palette */
    --gray-dark:#3D2F25; --gray-mid:#6B5440; --gray-light:#9A826A;
    --gray-border:#E4DED5; --gray-surface:#F5F0E8; --gray-bg:#FAF7F1;
  }
  *{box-sizing:border-box;margin:0;padding:0;}
  body{font-family:'DM Sans',sans-serif;background:var(--bg);color:var(--black);min-height:100vh;font-size:14px;}

  /* Admin banner */
  .admin-banner{background:#2E241D;color:#F5F0E8;padding:6px 24px;font-size:12px;display:none;align-items:center;justify-content:space-between;border-bottom:1px solid #3D2F25;}
  .admin-banner.visible{display:flex;}
  .admin-banner-left{display:flex;align-items:center;gap:8px;}
  .admin-pill{background:var(--amber);color:white;font-size:10px;font-weight:600;padding:2px 8px;border-radius:20px;letter-spacing:0.04em;text-transform:uppercase;}

  /* Header */
  .header{background:var(--black);padding:0 24px;height:54px;display:flex;align-items:center;justify-content:space-between;}
  .header-left{display:flex;align-items:center;gap:14px;}
  .header-logo-wrap{width:28px;height:28px;display:flex;align-items:center;justify-content:center;background:var(--gold);border-radius:6px;}
  .header-logo-wrap svg{width:18px;height:18px;color:var(--black);}
  .header-divider{width:1px;height:22px;background:rgba(255,255,255,0.15);}
  .header-brand{color:white;font-size:13px;font-weight:500;}
  .header-brand span{color:rgba(255,255,255,0.4);font-weight:300;}
  .header-client{color:rgba(255,255,255,0.32);font-size:12px;margin-top:1px;}
  .user-tabs{display:flex;gap:4px;}
  .user-tabs.hidden{display:none;}
  .user-tab{padding:4px 14px;border-radius:20px;font-size:11px;font-weight:500;cursor:pointer;border:1px solid rgba(255,255,255,0.15);color:rgba(255,255,255,0.45);background:transparent;transition:all 0.15s;font-family:'DM Sans',sans-serif;}
  .user-tab.active{background:rgba(255,255,255,0.1);color:white;border-color:rgba(255,255,255,0.35);}
  .current-user-label{color:rgba(255,255,255,0.6);font-size:12px;font-weight:500;}

  /* Nav */
  .nav{background:var(--white);border-bottom:1px solid var(--border);padding:0 24px;display:flex;overflow-x:auto;}
  .nav-item{padding:13px 15px;font-size:13px;color:var(--brown-mid);cursor:pointer;border-bottom:2px solid transparent;white-space:nowrap;transition:all 0.15s;}
  .nav-item.hidden{display:none;}
  .nav-item:hover{color:var(--black);}
  .nav-item.active{color:var(--black);border-bottom-color:var(--gold);font-weight:500;}
  .nav-badge{background:var(--red);color:white;font-size:10px;font-weight:600;padding:1px 6px;border-radius:10px;margin-left:4px;}

  .main{padding:20px 24px 40px;max-width:1500px;margin:0 auto;}
  .page{display:none;}
  .page.active{display:block;}

  /* Demo banner */
  .demo-banner{background:var(--surface);border:1px solid var(--border);border-radius:8px;padding:8px 14px;margin-bottom:16px;font-size:12px;color:var(--brown-mid);display:flex;align-items:center;gap:8px;}
  .demo-dot{width:7px;height:7px;border-radius:50%;background:var(--amber);flex-shrink:0;}

  /* Stats */
  .stats-row{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:16px;}
  .stat-card{background:var(--white);border:1px solid var(--border);border-radius:10px;padding:16px 18px;}
  .stat-label{font-size:11px;color:var(--brown-mid);text-transform:uppercase;letter-spacing:0.06em;font-weight:500;}
  .stat-value{font-size:24px;font-weight:500;margin-top:6px;color:var(--black);}
  .stat-sub{font-size:12px;color:var(--brown-light);margin-top:2px;}
  .stat-up{color:var(--teal);}

  /* Cards */
  .two-col{display:grid;grid-template-columns:1fr 1fr;gap:16px;}
  .card{background:var(--white);border:1px solid var(--border);border-radius:10px;overflow:hidden;}
  .card-header{padding:12px 18px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;}
  .card-title{font-size:13px;font-weight:600;color:var(--black);}
  .kpi-row{display:flex;justify-content:space-between;align-items:center;padding:9px 18px;border-bottom:1px solid var(--border);font-size:13px;}
  .kpi-row:last-child{border-bottom:none;}
  .kpi-label{color:var(--brown-mid);}
  .kpi-value{font-weight:500;color:var(--black);}
  .row{display:flex;align-items:center;padding:9px 18px;border-bottom:1px solid var(--border);}
  .row:last-child{border-bottom:none;}
  .row-name{font-size:13px;color:var(--black);min-width:160px;}

  /* Badges */
  .badge{display:inline-block;font-size:10px;font-weight:500;padding:2px 8px;border-radius:20px;}
  .b-won{background:var(--success-bg);color:var(--success);}
  .b-pending{background:var(--amber-light);color:#7A4A08;}
  .b-lost{background:var(--red-light);color:#922B21;}
  .b-new{background:var(--blue-light);color:var(--blue-text);}

  .btn{font-family:'DM Sans',sans-serif;font-size:12px;padding:5px 12px;border-radius:6px;border:none;cursor:pointer;font-weight:500;transition:all 0.15s;}
  .btn-approve{background:var(--success-bg);color:var(--success);}
  .btn-adjust{background:var(--amber-light);color:#7A4A08;margin-left:6px;}
  .btn-primary{background:var(--black);color:white;padding:8px 16px;}
  .btn-primary:hover{opacity:0.85;}
  .btn-primary:disabled{opacity:0.4;cursor:not-allowed;}
  .btn-gold{background:var(--gold);color:var(--black);padding:8px 16px;font-weight:600;}
  .btn-gold:hover{background:#A27826;}

  .alert-bar{background:var(--red-light);border:1px solid #E8A89F;border-radius:8px;padding:10px 14px;margin-bottom:16px;display:flex;align-items:center;gap:10px;font-size:13px;color:#8B2A1E;}
  .alert-dot{width:8px;height:8px;border-radius:50%;background:var(--red);flex-shrink:0;}

  .owner-lock{display:inline-flex;align-items:center;gap:5px;font-size:11px;padding:4px 10px;border-radius:6px;background:var(--surface);color:var(--brown-mid);border:1px solid var(--border);font-weight:500;}
  .owner-lock svg{width:11px;height:11px;flex-shrink:0;}

  /* ── LUMBER BUDDY TAB ── */
  .buddy-layout{display:grid;grid-template-columns:1fr 280px;gap:16px;height:calc(100vh - 142px);min-height:540px;}

  .chat-outer{border:1px solid var(--border);border-radius:10px;overflow:hidden;background:var(--white);display:flex;flex-direction:column;}
  .chat-header{background:var(--black);padding:12px 16px;display:flex;align-items:center;gap:10px;flex-shrink:0;}
  .chat-logo-wrap{width:30px;height:30px;display:flex;align-items:center;justify-content:center;background:var(--gold);border-radius:6px;flex-shrink:0;}
  .chat-logo-wrap svg{width:18px;height:18px;color:var(--black);}
  .chat-header-text{flex:1;}
  .chat-title{color:white;font-size:13px;font-weight:500;line-height:1.3;}
  .chat-subtitle{color:rgba(255,255,255,0.4);font-size:11px;}
  .online-dot{width:7px;height:7px;background:#2ECC71;border-radius:50%;}

  .chat-messages{flex:1;overflow-y:auto;padding:16px;display:flex;flex-direction:column;gap:12px;}
  .msg{display:flex;gap:10px;align-items:flex-start;}
  .msg.user{flex-direction:row-reverse;}
  .msg-av{width:30px;height:30px;border-radius:6px;display:flex;align-items:center;justify-content:center;flex-shrink:0;margin-top:1px;}
  .msg-av.brain{background:var(--gold);}
  .msg-av.brain svg{width:18px;height:18px;color:var(--black);}
  .msg-av.user{background:var(--surface);color:var(--brown-mid);font-size:11px;font-weight:600;border-radius:50%;}
  .msg-bubble{max-width:80%;padding:10px 14px;border-radius:10px;font-size:13px;line-height:1.7;}
  .msg.brain .msg-bubble{background:var(--surface);color:var(--black);border-radius:2px 10px 10px 10px;border:1px solid var(--border);}
  .msg.user .msg-bubble{background:var(--black);color:white;border-radius:10px 2px 10px 10px;}

  /* Msg actions (for Send to CEO button on brain messages) */
  .msg-actions{display:flex;gap:6px;margin-top:6px;padding-left:40px;}
  .msg-action-btn{font-size:11px;padding:4px 10px;border-radius:14px;border:1px solid var(--border);background:var(--white);color:var(--brown-mid);cursor:pointer;font-family:'DM Sans',sans-serif;transition:all 0.15s;}
  .msg-action-btn:hover{border-color:var(--gold);color:var(--black);background:var(--gold-light);}
  .msg-action-btn:disabled{opacity:0.5;cursor:not-allowed;}

  /* Quick prompts */
  .quick-strip{padding:8px 14px 0;display:flex;gap:6px;flex-wrap:wrap;flex-shrink:0;border-top:1px solid var(--border);}
  .qchip{font-size:11px;padding:4px 10px;border-radius:20px;border:1px solid var(--border);background:var(--white);color:var(--brown-mid);cursor:pointer;white-space:nowrap;transition:all 0.15s;font-family:'DM Sans',sans-serif;}
  .qchip:hover{border-color:var(--black);color:var(--black);background:var(--surface);}

  .chat-input-area{padding:10px 14px;display:flex;gap:8px;flex-shrink:0;}
  .chat-input-area textarea{flex:1;border:1px solid var(--border);border-radius:8px;padding:8px 10px;font-family:'DM Sans',sans-serif;font-size:13px;resize:none;outline:none;line-height:1.5;background:var(--bg);color:var(--black);}
  .chat-input-area textarea:focus{border-color:var(--brown-dark);background:var(--white);}
  .send-btn{width:38px;height:38px;border-radius:8px;background:var(--black);border:none;color:white;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;transition:opacity 0.15s;}
  .send-btn:hover{opacity:0.85;}
  .send-btn:disabled{background:var(--border);cursor:not-allowed;}

  /* Sidebar */
  .buddy-sidebar{display:flex;flex-direction:column;gap:12px;overflow-y:auto;}
  .conn-card{background:var(--white);border:1px solid var(--border);border-radius:10px;overflow:hidden;}
  .conn-card-header{padding:11px 14px;border-bottom:1px solid var(--border);font-size:11px;font-weight:600;color:var(--brown-mid);text-transform:uppercase;letter-spacing:0.07em;}
  .conn-row{display:flex;align-items:center;justify-content:space-between;padding:9px 14px;border-bottom:1px solid var(--border);}
  .conn-row:last-child{border-bottom:none;}
  .conn-name{font-size:13px;color:var(--black);}
  .conn-detail{font-size:11px;color:var(--brown-light);margin-top:1px;}
  .conn-status{font-size:10px;font-weight:500;padding:2px 8px;border-radius:20px;}
  .cs-live{background:var(--teal-light);color:#085041;}
  .cs-pending{background:var(--surface);color:var(--brown-mid);}
  .cs-tool-live{background:var(--gold-light);color:#7A5808;border:1px solid var(--gold);}

  /* ══ PRICING TAB ══ */
  .pricing-layout{display:grid;grid-template-columns:1fr 340px;gap:16px;}
  .calc-card{background:var(--white);border:1px solid var(--border);border-radius:10px;overflow:hidden;}
  .calc-card-header{padding:14px 18px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;}
  .calc-card-header .live-pill{font-size:10px;font-weight:600;padding:3px 10px;border-radius:20px;background:var(--gold-light);color:#7A5808;border:1px solid var(--gold);}
  .calc-body{padding:18px;}
  .calc-row{display:grid;grid-template-columns:2fr 90px 90px auto;gap:10px;margin-bottom:10px;align-items:center;}
  .calc-row select,.calc-row input{font-family:'DM Sans',sans-serif;font-size:13px;padding:7px 10px;border:1px solid var(--border);border-radius:6px;background:var(--white);color:var(--black);outline:none;}
  .calc-row select:focus,.calc-row input:focus{border-color:var(--brown-dark);}
  .calc-row .line-total{font-size:13px;color:var(--black);font-weight:500;text-align:right;padding-right:4px;}
  .calc-row .remove-btn{width:24px;height:24px;border:none;background:var(--surface);color:var(--brown-mid);border-radius:4px;cursor:pointer;font-size:14px;line-height:1;}
  .calc-row .remove-btn:hover{background:var(--red-light);color:var(--red);}
  .calc-add-btn{font-size:12px;padding:6px 12px;border-radius:6px;border:1px dashed var(--brown-light);background:transparent;color:var(--brown-mid);cursor:pointer;margin-top:4px;font-family:'DM Sans',sans-serif;}
  .calc-add-btn:hover{border-color:var(--black);color:var(--black);}

  .calc-settings{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:16px;padding-top:16px;border-top:1px solid var(--border);}
  .calc-field{display:flex;flex-direction:column;gap:4px;}
  .calc-field label{font-size:11px;color:var(--brown-mid);text-transform:uppercase;letter-spacing:0.06em;font-weight:500;}
  .calc-field input,.calc-field select{font-family:'DM Sans',sans-serif;font-size:13px;padding:7px 10px;border:1px solid var(--border);border-radius:6px;background:var(--white);outline:none;}

  .quote-summary{background:var(--white);border:1px solid var(--border);border-radius:10px;padding:18px;position:sticky;top:16px;align-self:flex-start;}
  .qs-label{font-size:11px;color:var(--brown-mid);text-transform:uppercase;letter-spacing:0.06em;font-weight:500;margin-bottom:6px;}
  .qs-total-box{padding:14px 16px;background:var(--black);color:white;border-radius:8px;margin-bottom:14px;}
  .qs-total-box .qs-total-label{font-size:11px;color:rgba(255,255,255,0.55);text-transform:uppercase;letter-spacing:0.08em;font-weight:500;}
  .qs-total-box .qs-total-val{font-size:26px;font-weight:500;margin-top:4px;}
  .qs-breakdown{font-size:12px;}
  .qs-line{display:flex;justify-content:space-between;padding:4px 0;color:var(--brown-mid);}
  .qs-line.strong{color:var(--black);font-weight:500;border-top:1px solid var(--border);margin-top:4px;padding-top:8px;}
  .qs-hint{font-size:11px;color:var(--brown-light);margin-top:10px;line-height:1.5;}

  /* ══ SALES/OPS/OTHER TABS ══ */
  .op-row{display:flex;justify-content:space-between;align-items:center;padding:10px 18px;border-bottom:1px solid var(--border);}
  .op-row:last-child{border-bottom:none;}
  .op-row-left{display:flex;gap:12px;align-items:flex-start;}
  .op-avatar{width:34px;height:34px;border-radius:6px;background:var(--gold-light);color:var(--brown-dark);display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:600;flex-shrink:0;}
  .op-name{font-size:13px;font-weight:500;}
  .op-detail{font-size:12px;color:var(--brown-mid);margin-top:2px;}

  .approval-actions{display:flex;align-items:center;gap:0;}

  /* My Route tab */
  .route-stop{display:flex;gap:14px;padding:16px 18px;border-bottom:1px solid var(--border);align-items:flex-start;}
  .route-stop:last-child{border-bottom:none;}
  .stop-num{width:28px;height:28px;border-radius:50%;background:var(--black);color:white;font-size:13px;font-weight:600;display:flex;align-items:center;justify-content:center;flex-shrink:0;}
  .stop-info{flex:1;}
  .stop-name{font-size:13px;font-weight:500;}
  .stop-addr{font-size:12px;color:var(--brown-mid);margin-top:2px;}
  .stop-product{font-size:12px;color:var(--brown-mid);margin-top:4px;}
  .stop-note{font-size:11px;color:var(--amber);margin-top:4px;background:var(--amber-light);padding:3px 8px;border-radius:4px;display:inline-block;}

  /* Settings */
  .settings-section{margin-bottom:24px;}
  .settings-section.hidden{display:none;}
  .settings-title{font-size:13px;font-weight:600;color:var(--black);margin-bottom:12px;}
  .setting-row{display:flex;justify-content:space-between;align-items:center;padding:12px 18px;background:var(--white);border:1px solid var(--border);border-bottom:none;}
  .setting-row:first-of-type{border-radius:10px 10px 0 0;}
  .setting-row:last-of-type{border-radius:0 0 10px 10px;border-bottom:1px solid var(--border);}
  .sr-label{font-size:13px;font-weight:500;color:var(--black);}
  .sr-detail{font-size:12px;color:var(--brown-mid);margin-top:2px;}
  .sr-right{display:flex;align-items:center;gap:10px;}
  .connect-btn{font-size:12px;padding:5px 12px;border-radius:6px;border:1px solid var(--border);background:var(--white);color:var(--black);cursor:pointer;font-family:'DM Sans',sans-serif;}
  .connect-btn:hover{border-color:var(--black);background:var(--surface);}
  .toggle{width:36px;height:20px;background:var(--black);border-radius:10px;position:relative;cursor:pointer;flex-shrink:0;}
  .toggle::after{content:'';position:absolute;top:2px;right:2px;width:16px;height:16px;background:white;border-radius:50%;transition:transform 0.15s;}
  .toggle.off{background:var(--border);}
  .toggle.off::after{transform:translateX(-16px);}

  /* Modal (Send to CEO) */
  .modal-overlay{position:fixed;inset:0;background:rgba(26,20,16,0.55);display:none;align-items:center;justify-content:center;z-index:100;padding:20px;}
  .modal-overlay.visible{display:flex;}
  .modal{background:var(--white);border-radius:12px;width:100%;max-width:520px;max-height:90vh;overflow-y:auto;box-shadow:0 20px 60px rgba(0,0,0,0.25);}
  .modal-header{padding:16px 20px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;}
  .modal-title{font-size:15px;font-weight:600;color:var(--black);}
  .modal-close{background:none;border:none;font-size:20px;cursor:pointer;color:var(--brown-mid);line-height:1;padding:4px 8px;}
  .modal-body{padding:20px;}
  .modal-field{margin-bottom:14px;}
  .modal-field label{display:block;font-size:11px;color:var(--brown-mid);text-transform:uppercase;letter-spacing:0.06em;font-weight:500;margin-bottom:4px;}
  .modal-field input,.modal-field textarea{width:100%;font-family:'DM Sans',sans-serif;font-size:13px;padding:9px 12px;border:1px solid var(--border);border-radius:6px;background:var(--white);outline:none;line-height:1.5;}
  .modal-field input:focus,.modal-field textarea:focus{border-color:var(--brown-dark);}
  .modal-field textarea{min-height:180px;resize:vertical;}
  .modal-footer{padding:14px 20px;border-top:1px solid var(--border);display:flex;justify-content:flex-end;gap:8px;}
  .modal-status{font-size:12px;padding:8px 12px;border-radius:6px;margin-bottom:12px;display:none;}
  .modal-status.success{background:var(--success-bg);color:var(--success);display:block;}
  .modal-status.error{background:var(--red-light);color:var(--red);display:block;}
  .modal-status.sending{background:var(--amber-light);color:#7A4A08;display:block;}

  .hidden{display:none !important;}

  /* Scrollbar */
  .chat-messages::-webkit-scrollbar,.buddy-sidebar::-webkit-scrollbar{width:6px;}
  .chat-messages::-webkit-scrollbar-thumb,.buddy-sidebar::-webkit-scrollbar-thumb{background:var(--border);border-radius:3px;}
</style>
</head>
<body>

<!-- Admin banner — only visible in admin mode -->
<div class="admin-banner" id="admin-banner">
  <div class="admin-banner-left">
    <span class="admin-pill">Admin</span>
    <span>Previewing as: <strong id="admin-current-role">Operations</strong></span>
  </div>
  <div style="color:rgba(255,255,255,0.4);font-size:11px;">Use the role toggle to switch views</div>
</div>

<div class="header">
  <div class="header-left">
    <!-- Wordmark: abstract stacked-logs icon -->
    <div class="header-logo-wrap">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="6" cy="8" r="3"/>
        <circle cx="12" cy="8" r="3"/>
        <circle cx="18" cy="8" r="3"/>
        <circle cx="9" cy="16" r="3"/>
        <circle cx="15" cy="16" r="3"/>
      </svg>
    </div>
    <div class="header-divider"></div>
    <div>
      <div class="header-brand">Lumber Buddy <span>by Obsidian Labs</span></div>
      <div class="header-client">Musser Biomass</div>
    </div>
  </div>
  <div class="user-tabs hidden" id="user-tabs">
    <button class="user-tab active" data-role="ops" onclick="switchRole('ops',this)">Operations</button>
    <button class="user-tab" data-role="owner" onclick="switchRole('owner',this)">Owner</button>
    <button class="user-tab" data-role="yard" onclick="switchRole('yard',this)">Yard</button>
    <button class="user-tab" data-role="driver" onclick="switchRole('driver',this)">Driver</button>
  </div>
  <div class="current-user-label" id="current-user-label" style="display:none;"></div>
</div>

<div class="nav" id="main-nav">
  <div class="nav-item active" data-tab="buddy" onclick="showPage('buddy',this)">Lumber Buddy</div>
  <div class="nav-item" data-tab="operations" onclick="showPage('operations',this)">Operations</div>
  <div class="nav-item" data-tab="sales" onclick="showPage('sales',this)">Sales <span class="nav-badge" id="approval-badge">2</span></div>
  <div class="nav-item" data-tab="pricing" onclick="showPage('pricing',this)">Pricing</div>
  <div class="nav-item" data-tab="marketing" onclick="showPage('marketing',this)">Marketing</div>
  <div class="nav-item" data-tab="finance" onclick="showPage('finance',this)">Finance</div>
  <div class="nav-item" data-tab="strategy" onclick="showPage('strategy',this)">Strategy</div>
  <div class="nav-item" data-tab="myroute" onclick="showPage('myroute',this)">My Route</div>
  <div class="nav-item" data-tab="settings" onclick="showPage('settings',this)">⚙ Settings</div>
</div>

<div class="main">

<!-- ══ LUMBER BUDDY ══ -->
<div class="page active" id="page-buddy">
  <div class="buddy-layout">
    <div class="chat-outer">
      <div class="chat-header">
        <div class="chat-logo-wrap">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="6" cy="8" r="3"/><circle cx="12" cy="8" r="3"/><circle cx="18" cy="8" r="3"/>
            <circle cx="9" cy="16" r="3"/><circle cx="15" cy="16" r="3"/>
          </svg>
        </div>
        <div class="chat-header-text">
          <div class="chat-title">Lumber Buddy</div>
          <div class="chat-subtitle">Musser Biomass · AI Brain</div>
        </div>
        <div class="online-dot"></div>
      </div>
      <div class="chat-messages" id="chat-messages"></div>
      <div class="quick-strip" id="quick-strip"></div>
      <div class="chat-input-area">
        <textarea id="chat-input" placeholder="Ask Lumber Buddy anything about Musser Biomass..." rows="1" onkeydown="handleKey(event)"></textarea>
        <button class="send-btn" id="send-btn" onclick="sendMsg()">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><line x1="1" y1="7" x2="13" y2="7"/><polyline points="8,2 13,7 8,12"/></svg>
        </button>
      </div>
    </div>
    <div class="buddy-sidebar">
      <div class="conn-card">
        <div class="conn-card-header">Live tools</div>
        <div class="conn-row"><div><div class="conn-name">Pricing Calculator</div><div class="conn-detail">Reads PRICING.md · /api/quote</div></div><span class="conn-status cs-tool-live">Live</span></div>
        <div class="conn-row"><div><div class="conn-name">Send to CEO</div><div class="conn-detail">Via Resend · /api/send-email</div></div><span class="conn-status cs-tool-live">Live</span></div>
      </div>
      <div class="conn-card">
        <div class="conn-card-header">Data connections</div>
        <div class="conn-row"><div><div class="conn-name">GoHighLevel CRM</div><div class="conn-detail">Contacts, pipeline, Conversation AI</div></div><span class="conn-status cs-pending">Pending</span></div>
        <div class="conn-row"><div><div class="conn-name">QuickBooks</div><div class="conn-detail">Revenue + invoicing sync</div></div><span class="conn-status cs-pending">Pending</span></div>
        <div class="conn-row"><div><div class="conn-name">Inventory DB</div><div class="conn-detail">Loads, bags, bales · Supabase</div></div><span class="conn-status cs-pending">Pending</span></div>
        <div class="conn-row"><div><div class="conn-name">Project docs</div><div class="conn-detail">Living docs · GitHub</div></div><span class="conn-status cs-live">Live</span></div>
      </div>
    </div>
  </div>
</div>

<!-- ══ OPERATIONS ══ -->
<div class="page" id="page-operations">
  <div class="demo-banner"><div class="demo-dot"></div>Demo mode — production + load schedule will sync from QuickBooks/Supabase once connected.</div>
  <div class="stats-row">
    <div class="stat-card"><div class="stat-label">Loads scheduled this week</div><div class="stat-value">9</div><div class="stat-sub">5 pellets · 3 briquettes · 1 fiber</div></div>
    <div class="stat-card"><div class="stat-label">Trucks committed</div><div class="stat-value">9 of 12</div><div class="stat-sub">3 open slots · room for spot loads</div></div>
    <div class="stat-card"><div class="stat-label">Production rate</div><div class="stat-value">2.1 loads/day</div><div class="stat-sub">Pellet line running · briquette press up</div></div>
    <div class="stat-card"><div class="stat-label">On-time delivery (30d)</div><div class="stat-value">96%</div><div class="stat-sub">2 late loads — driver-side delays</div></div>
  </div>
  <div class="two-col">
    <div class="card">
      <div class="card-header"><div class="card-title">This week's loads</div><span style="font-size:12px;color:var(--brown-light)">Sorted by ship date</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">1</div><div><div class="op-name">Town &amp; Country Store</div><div class="op-detail">1 load Forest Fuel pellets · Hamilton NY · Tue</div></div></div><span class="badge b-won">Loaded</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">2</div><div><div class="op-name">TJ Coal &amp; Stove</div><div class="op-detail">1 load Forest Fuel briquettes · Spartansburg PA · Wed</div></div></div><span class="badge b-won">Scheduled</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">3</div><div><div class="op-name">Town &amp; Country Store</div><div class="op-detail">1 load Alpha Fiber · Hamilton NY · Thu</div></div></div><span class="badge b-won">Scheduled</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">4</div><div><div class="op-name">TJ Coal &amp; Stove</div><div class="op-detail">2 loads Forest Fuel briquettes · Spartansburg PA · Fri</div></div></div><span class="badge b-pending">Pressing</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">5</div><div><div class="op-name">Town &amp; Country Store</div><div class="op-detail">1 load Forest Fuel pellets · Hamilton NY · Fri</div></div></div><span class="badge b-new">Queued</span></div>
    </div>
    <div class="card">
      <div class="card-header"><div class="card-title">Production pulse</div><span style="font-size:12px;color:var(--brown-light)">Finished goods on hand</span></div>
      <div class="kpi-row"><span class="kpi-label">Forest Fuel pellets (loads ready)</span><span class="kpi-value">4 <span style="color:var(--teal);font-size:11px">healthy</span></span></div>
      <div class="kpi-row"><span class="kpi-label">Forest Fuel briquettes (loads ready)</span><span class="kpi-value">2 <span style="color:var(--amber);font-size:11px">low · press queue Wed</span></span></div>
      <div class="kpi-row"><span class="kpi-label">Alpha Fiber (loads ready)</span><span class="kpi-value">3 <span style="color:var(--teal);font-size:11px">healthy</span></span></div>
      <div class="kpi-row"><span class="kpi-label">Pellet bags on hand (40 lb)</span><span class="kpi-value">~4,400</span></div>
      <div class="kpi-row"><span class="kpi-label">Briquette 6-packs on hand</span><span class="kpi-value">~4,200</span></div>
      <div class="kpi-row"><span class="kpi-label">Alpha Fiber bales on hand (25 lb)</span><span class="kpi-value">~3,500</span></div>
      <div class="kpi-row"><span class="kpi-label">Raw fiber inventory</span><span class="kpi-value">11 days <span style="color:var(--amber);font-size:11px">reorder window opening</span></span></div>
    </div>
  </div>
</div>

<!-- ══ SALES ══ -->
<div class="page" id="page-sales">
  <div class="alert-bar"><div class="alert-dot"></div><span id="sales-alert-text">2 quotes awaiting approval — <strong style="margin-left:4px">$24,540 total.</strong></span></div>
  <div class="stats-row">
    <div class="stat-card"><div class="stat-label">Open quotes</div><div class="stat-value">7</div><div class="stat-sub">$84,200 total value</div></div>
    <div class="stat-card"><div class="stat-label">Close rate (30d)</div><div class="stat-value">71%</div><div class="stat-sub stat-up">↑ 6% vs prior month</div></div>
    <div class="stat-card"><div class="stat-label">Avg load price</div><div class="stat-value">$5,734</div><div class="stat-sub">Product avg, freight separate</div></div>
    <div class="stat-card"><div class="stat-label">Pipeline value</div><div class="stat-value">$84,200</div><div class="stat-sub">7 open quotes</div></div>
  </div>
  <div class="two-col">
    <div class="card">
      <div class="card-header"><div class="card-title">Needs approval</div></div>
      <div class="op-row">
        <div class="op-row-left"><div class="op-avatar">NE</div><div><div class="op-name">Northeast Stove Supply (new wholesale)</div><div class="op-detail">2 loads Forest Fuel pellets · Binghamton NY · first-time</div><div style="font-size:13px;margin-top:4px;font-weight:500">$13,178 quote (incl. freight)</div></div></div>
        <div class="approval-actions"><button class="btn btn-approve" onclick="approveJob(this,13178)">Approve</button><button class="btn btn-adjust">Adjust</button></div>
      </div>
      <div class="op-row">
        <div class="op-row-left"><div class="op-avatar">AC</div><div><div class="op-name">Appalachian Coal &amp; Stove</div><div class="op-detail">1 load briquettes · Bristol TN · returning customer</div><div style="font-size:13px;margin-top:4px;font-weight:500">$5,861 quote (incl. freight)</div></div></div>
        <div class="approval-actions"><button class="btn btn-approve" onclick="approveJob(this,5861)">Approve</button><button class="btn btn-adjust">Adjust</button></div>
      </div>
    </div>
    <div class="card">
      <div class="card-header"><div class="card-title">Recent quotes sent</div></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">TC</div><div><div class="op-name">Town &amp; Country Store</div><div class="op-detail">3 loads pellets, restock · sent yesterday</div></div></div><span class="badge b-pending">Awaiting</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">TJ</div><div><div class="op-name">TJ Coal &amp; Stove</div><div class="op-detail">2 loads briquettes · sent 2 days ago</div></div></div><span class="badge b-won">Accepted</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">HV</div><div><div class="op-name">Hudson Valley Hearth</div><div class="op-detail">1 load pellets, sample order · sent 3 days ago</div></div></div><span class="badge b-pending">Awaiting</span></div>
      <div class="op-row"><div class="op-row-left"><div class="op-avatar">PA</div><div><div class="op-name">Pocono Pellet Co.</div><div class="op-detail">2 loads briquettes · sent 5 days ago</div></div></div><span class="badge b-won">Accepted</span></div>
    </div>
  </div>
</div>

<!-- ══ PRICING ══ (THE REAL TOOL) ══ -->
<div class="page" id="page-pricing">
  <div class="demo-banner"><div class="demo-dot" style="background:var(--teal)"></div>Live tool — calls <code style="font-size:11px;background:var(--surface);padding:1px 4px;border-radius:3px">/api/quote</code>. Mirrors PRICING.md. Same math as GHL Conversation AI.</div>
  <div class="pricing-layout">
    <div class="calc-card">
      <div class="calc-card-header">
        <div class="card-title">Quote builder</div>
        <span class="live-pill">Live · /api/quote</span>
      </div>
      <div class="calc-body">
        <div id="calc-lines"></div>
        <button class="calc-add-btn" onclick="addCalcLine()">+ Add line item</button>

        <div class="calc-settings">
          <div class="calc-field">
            <label>Origin ZIP</label>
            <input type="text" id="calc-from-zip" value="24368" maxlength="5" readonly style="background:var(--surface);color:var(--brown-mid);cursor:not-allowed" title="Locked — Musser ships from 24368">
          </div>
          <div class="calc-field">
            <label>Destination ZIP</label>
            <input type="text" id="calc-to-zip" placeholder="13346" maxlength="5" inputmode="numeric" pattern="\d{5}" onchange="recalcQuote()" oninput="if(this.value.length===5) recalcQuote()">
          </div>
          <div class="calc-field">
            <label>Customer type</label>
            <select id="calc-customer-type" onchange="recalcQuote()">
              <option value="retail">Retail</option>
              <option value="commercial">Commercial</option>
              <option value="wholesale">Wholesale</option>
            </select>
          </div>
        </div>
        <div id="calc-distance-info" style="margin-top:8px;padding:10px 12px;background:var(--surface);border-radius:6px;font-size:13px;color:var(--brown-mid);display:none"></div>
      </div>
    </div>

    <div class="quote-summary">
      <div class="qs-label">Quote total</div>
      <div class="qs-total-box">
        <div class="qs-total-label">Grand total</div>
        <div class="qs-total-val" id="qs-grand-total">$0.00</div>
      </div>
      <div class="qs-breakdown" id="qs-breakdown">
        <div class="qs-line"><span>Subtotal</span><span>$0.00</span></div>
        <div class="qs-line"><span>Delivery</span><span>$0.00</span></div>
      </div>
      <div class="qs-hint">Logic mirrors <code style="font-size:11px">PRICING.md</code>. Paste the same doc into the GHL Conversation AI system prompt so chat/SMS quotes match these numbers.</div>
    </div>
  </div>
</div>

<!-- ══ MARKETING ══ -->
<div class="page" id="page-marketing">
  <div class="stats-row">
    <div class="stat-card"><div class="stat-label">Leads this month</div><div class="stat-value">44</div><div class="stat-sub stat-up">↑ 12% vs April '25</div></div>
    <div class="stat-card"><div class="stat-label">Cost per lead</div><div class="stat-value">$28</div><div class="stat-sub stat-up">↓ $6 vs last month</div></div>
    <div class="stat-card"><div class="stat-label">Best source</div><div class="stat-value">Google</div><div class="stat-sub">38% of leads · strong reviews</div></div>
    <div class="stat-card"><div class="stat-label">Repeat rate</div><div class="stat-value">31%</div><div class="stat-sub">Up from 24% a year ago</div></div>
  </div>
  <div class="two-col">
    <div class="card">
      <div class="card-header"><div class="card-title">Lead sources</div><span style="font-size:12px;color:var(--brown-light)">Last 30 days</span></div>
      <div class="row"><div class="row-name">Google Business</div><div style="flex:1;padding:0 12px"><div style="height:5px;background:var(--surface);border-radius:3px"><div style="width:80%;height:100%;background:var(--black);border-radius:3px"></div></div></div><span style="font-size:12px;color:var(--brown-mid);min-width:60px;text-align:right">17 leads</span><span style="font-size:12px;font-weight:500;min-width:40px;text-align:right">38%</span></div>
      <div class="row"><div class="row-name">Repeat / referral</div><div style="flex:1;padding:0 12px"><div style="height:5px;background:var(--surface);border-radius:3px"><div style="width:65%;height:100%;background:var(--brown-dark);border-radius:3px"></div></div></div><span style="font-size:12px;color:var(--brown-mid);min-width:60px;text-align:right">14 leads</span><span style="font-size:12px;font-weight:500;min-width:40px;text-align:right">31%</span></div>
      <div class="row"><div class="row-name">Facebook</div><div style="flex:1;padding:0 12px"><div style="height:5px;background:var(--surface);border-radius:3px"><div style="width:48%;height:100%;background:var(--brown-mid);border-radius:3px"></div></div></div><span style="font-size:12px;color:var(--brown-mid);min-width:60px;text-align:right">10 leads</span><span style="font-size:12px;font-weight:500;min-width:40px;text-align:right">22%</span></div>
      <div class="row"><div class="row-name">Walk-up</div><div style="flex:1;padding:0 12px"><div style="height:5px;background:var(--surface);border-radius:3px"><div style="width:12%;height:100%;background:var(--brown-light);border-radius:3px"></div></div></div><span style="font-size:12px;color:var(--brown-mid);min-width:60px;text-align:right">3 leads</span><span style="font-size:12px;font-weight:500;min-width:40px;text-align:right">7%</span></div>
    </div>
    <div class="card">
      <div class="card-header"><div class="card-title">Source performance</div></div>
      <div class="kpi-row"><span class="kpi-label">Repeat close rate</span><span class="kpi-value" style="color:var(--teal)">67%</span></div>
      <div class="kpi-row"><span class="kpi-label">Google close rate</span><span class="kpi-value">42%</span></div>
      <div class="kpi-row"><span class="kpi-label">Facebook close rate</span><span class="kpi-value">28%</span></div>
      <div class="kpi-row"><span class="kpi-label">Avg order — repeat</span><span class="kpi-value">$780</span></div>
      <div class="kpi-row"><span class="kpi-label">Avg order — Google</span><span class="kpi-value">$540</span></div>
      <div class="kpi-row"><span class="kpi-label">Seasonal campaign lift</span><span class="kpi-value stat-up">+34% (Sept pre-buy)</span></div>
    </div>
  </div>
</div>

<!-- ══ FINANCE ══ -->
<div class="page" id="page-finance">
  <div class="stats-row">
    <div class="stat-card"><div class="stat-label">Revenue MTD</div><div class="stat-value">$112,400</div><div class="stat-sub stat-up">↑ 22% vs April '25</div></div>
    <div class="stat-card"><div class="stat-label">Outstanding</div><div class="stat-value">$36,200</div><div class="stat-sub">6 open invoices</div></div>
    <div class="stat-card"><div class="stat-label">Gross margin</div><div class="stat-value">31%</div><div class="stat-sub">Steady vs. last year</div></div>
    <div class="stat-card"><div class="stat-label">Projected month</div><div class="stat-value">$148,000</div><div class="stat-sub">At current run rate</div></div>
  </div>
  <div class="two-col">
    <div class="card">
      <div class="card-header"><div class="card-title">Revenue by product line</div><span style="font-size:12px;color:var(--brown-light)">MTD</span></div>
      <div class="kpi-row"><span class="kpi-label">Forest Fuel pellets</span><span class="kpi-value">$57,200</span></div>
      <div class="kpi-row"><span class="kpi-label">Forest Fuel briquettes</span><span class="kpi-value">$26,400</span></div>
      <div class="kpi-row"><span class="kpi-label">Alpha Fiber</span><span class="kpi-value">$18,600</span></div>
      <div class="kpi-row"><span class="kpi-label">Freight (pass-through)</span><span class="kpi-value">$10,200</span></div>
    </div>
    <div class="card">
      <div class="card-header"><div class="card-title">P&amp;L snapshot</div><span style="font-size:12px;color:var(--brown-light)">April MTD</span></div>
      <div class="kpi-row"><span class="kpi-label">Gross revenue</span><span class="kpi-value">$112,400</span></div>
      <div class="kpi-row"><span class="kpi-label">Raw fiber + materials</span><span class="kpi-value" style="color:var(--red)">−$42,300</span></div>
      <div class="kpi-row"><span class="kpi-label">Labor</span><span class="kpi-value" style="color:var(--red)">−$23,800</span></div>
      <div class="kpi-row"><span class="kpi-label">Energy + plant</span><span class="kpi-value" style="color:var(--red)">−$11,500</span></div>
      <div class="kpi-row"><span class="kpi-label">Gross margin</span><span class="kpi-value" style="color:var(--teal)">$34,800 (31%)</span></div>
      <div class="kpi-row"><span class="kpi-label">QuickBooks sync</span><span class="kpi-value" style="color:var(--amber)">Pending setup</span></div>
    </div>
  </div>
</div>

<!-- ══ STRATEGY ══ -->
<div class="page" id="page-strategy">
  <div class="stats-row">
    <div class="stat-card"><div class="stat-label">Annual target</div><div class="stat-value">$1.6M</div><div class="stat-sub">Current pace $1.5M</div></div>
    <div class="stat-card"><div class="stat-label">Wholesale accounts</div><div class="stat-value">12 → 24</div><div class="stat-sub">Stretch goal by Q4</div></div>
    <div class="stat-card"><div class="stat-label">AI-handled inquiries</div><div class="stat-value">60%</div><div class="stat-sub">Primary KPI</div></div>
    <div class="stat-card"><div class="stat-label">Avg load freight</div><div class="stat-value">$1,275</div><div class="stat-sub">~432 mi avg lane</div></div>
  </div>
  <div class="two-col">
    <div class="card">
      <div class="card-header"><div class="card-title">Big bets in flight</div></div>
      <div class="kpi-row"><span class="kpi-label">Northeast Stove Supply (NY chain)</span><span class="kpi-value">$220k/yr potential</span></div>
      <div class="kpi-row"><span class="kpi-label">Second pellet line (capital project)</span><span class="kpi-value">Q3 decision</span></div>
      <div class="kpi-row"><span class="kpi-label">Alpha Fiber bedding-market push</span><span class="kpi-value">Sample loads out</span></div>
      <div class="kpi-row"><span class="kpi-label">Pre-heating-season pellet pre-buy</span><span class="kpi-value">35% of annual pellets</span></div>
    </div>
    <div class="card">
      <div class="card-header"><div class="card-title">What the system solves</div></div>
      <div class="kpi-row"><span class="kpi-label">Owner pricing calls/day</span><span class="kpi-value">10–15 → goal 2–3</span></div>
      <div class="kpi-row"><span class="kpi-label">Quote consistency</span><span class="kpi-value">PRICING.md is the law</span></div>
      <div class="kpi-row"><span class="kpi-label">Load scheduling</span><span class="kpi-value">Whiteboard → app</span></div>
      <div class="kpi-row"><span class="kpi-label">Invoice lag</span><span class="kpi-value">Auto on delivery</span></div>
    </div>
  </div>
</div>

<!-- ══ MY ROUTE (driver view) ══ -->
<div class="page" id="page-myroute">
  <div class="demo-banner"><div class="demo-dot"></div>Driver view — today's loads in order. Mark each complete as you go.</div>
  <div class="card">
    <div class="card-header"><div class="card-title">Today's loads · Truck 2</div><span style="font-size:12px;color:var(--brown-light)">2 deliveries · long-haul day</span></div>
    <div class="route-stop">
      <div class="stop-num">1</div>
      <div class="stop-info">
        <div class="stop-name">Town &amp; Country Store</div>
        <div class="stop-addr">2433 Route 12B, Hamilton NY 13346 · ETA Tue 2:00 PM</div>
        <div class="stop-product">1 load Forest Fuel pellets (1,100 bags) · $7,259.90 net-30</div>
        <div class="stop-note">~522 mi from Pulaski · BOL signed at receiving</div>
      </div>
    </div>
    <div class="route-stop">
      <div class="stop-num">2</div>
      <div class="stop-info">
        <div class="stop-name">Yard return</div>
        <div class="stop-addr">Musser yard, 24368 · Wed AM</div>
        <div class="stop-product">Drop paperwork, park Truck 2, fuel if under ¼</div>
        <div class="stop-note">Pre-trip Wed afternoon for Spartansburg PA run</div>
      </div>
    </div>
  </div>
</div>

<!-- ══ SETTINGS ══ -->
<div class="page" id="page-settings">
  <div class="settings-section">
    <div class="settings-title">Integrations</div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">GoHighLevel CRM</div><div class="sr-detail">Contacts, pipeline, Conversation AI (chat/SMS quoting)</div></div><div class="sr-right"><span style="font-size:12px;color:var(--amber)">Pending setup</span><button class="connect-btn">Configure</button></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">QuickBooks Online</div><div class="sr-detail">Revenue, invoicing, P&L sync</div></div><div class="sr-right"><span style="font-size:12px;color:var(--brown-light)">Credentials needed</span><button class="connect-btn">Connect</button></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Supabase Inventory</div><div class="sr-detail">Load, bag, bale stock — live counts</div></div><div class="sr-right"><span style="font-size:12px;color:var(--brown-light)">Not configured</span><button class="connect-btn">Connect</button></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Resend — Email</div><div class="sr-detail">Sending domain for /api/send-email</div></div><div class="sr-right"><span style="font-size:12px;color:var(--teal)">Live · tool active</span><button class="connect-btn">Configure</button></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Google Maps</div><div class="sr-detail">Route optimization for dispatch</div></div><div class="sr-right"><span style="font-size:12px;color:var(--brown-light)">Not connected</span><button class="connect-btn">Connect</button></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">GitHub — Project docs</div><div class="sr-detail">Living docs · brain reads automatically (incl. PRICING.md)</div></div><div class="sr-right"><span style="font-size:12px;color:var(--teal)">Live</span><button class="connect-btn">Configure</button></div></div>
  </div>
  <div class="settings-section">
    <div class="settings-title">Users</div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">CEO / Owner</div><div class="sr-detail">Full access · approves pricing · receives Lumber Buddy emails</div></div><div class="sr-right"><span class="badge b-won">Active</span></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Operations Manager</div><div class="sr-detail">Dispatch, scheduling, customer comms</div></div><div class="sr-right"><span class="badge b-won">Active</span></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Yard Lead</div><div class="sr-detail">Production, splitter, kiln schedule</div></div><div class="sr-right"><span class="badge b-won">Active</span></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Drivers</div><div class="sr-detail">My Route view only · mobile-first</div></div><div class="sr-right"><span class="badge b-won">3 active</span></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Add user</div><div class="sr-detail">Invite a team member</div></div><div class="sr-right"><button class="connect-btn">+ Invite</button></div></div>
  </div>
  <div class="settings-section" id="brain-settings-section">
    <div class="settings-title">Brain settings</div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Weekly CEO summary</div><div class="sr-detail">Lumber Buddy drafts and emails a Monday report</div></div><div class="sr-right"><div class="toggle off" onclick="this.classList.toggle('off')"></div></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Pricing update alerts</div><div class="sr-detail">Notify owner when PRICING.md is edited</div></div><div class="sr-right"><div class="toggle" onclick="this.classList.toggle('off')"></div></div></div>
    <div class="setting-row"><div class="sr-left"><div class="sr-label">Approval notifications</div><div class="sr-detail">Alert owner when a wholesale/custom quote needs review</div></div><div class="sr-right"><div class="toggle" onclick="this.classList.toggle('off')"></div></div></div>
  </div>
</div>

</div><!-- /main -->

<!-- Send to CEO modal -->
<div class="modal-overlay" id="email-modal">
  <div class="modal">
    <div class="modal-header">
      <div class="modal-title">Send to CEO</div>
      <button class="modal-close" onclick="closeEmailModal()">×</button>
    </div>
    <div class="modal-body">
      <div class="modal-status" id="email-status"></div>
      <div class="modal-field">
        <label>Subject</label>
        <input type="text" id="email-subject" placeholder="e.g. Weekly revenue snapshot">
      </div>
      <div class="modal-field">
        <label>Message</label>
        <textarea id="email-body" placeholder="Draft the email body..."></textarea>
      </div>
      <div style="font-size:11px;color:var(--brown-light);line-height:1.5;">
        Sends via Resend to the address set in the <code style="font-size:11px">CEO_EMAIL</code> env var. Make sure <code style="font-size:11px">RESEND_API_KEY</code>, <code style="font-size:11px">FROM_EMAIL</code>, and <code style="font-size:11px">CEO_EMAIL</code> are all set in Vercel first.
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn" style="background:var(--surface);color:var(--brown-mid)" onclick="closeEmailModal()">Cancel</button>
      <button class="btn btn-gold" id="email-send-btn" onclick="sendEmailToCeo()">Send to CEO</button>
    </div>
  </div>
</div>

<script>
// ══════════════════════════════════════════════════════════════════════
// Lumber Buddy — client script
// ══════════════════════════════════════════════════════════════════════

// ── ROLE DEFINITIONS ──
const ROLES = {
  owner: {
    displayName: 'Owner',
    visibleTabs: ['buddy','sales','pricing','marketing','finance','strategy','settings'],
    defaultTab: 'buddy',
    canApprove: true,
    seeBrainSettings: true
  },
  ops: {
    displayName: 'Operations',
    visibleTabs: ['buddy','operations','sales','pricing','marketing','settings'],
    defaultTab: 'buddy',
    canApprove: false,
    seeBrainSettings: false
  },
  yard: {
    displayName: 'Yard',
    visibleTabs: ['buddy','operations'],
    defaultTab: 'operations',
    canApprove: false,
    seeBrainSettings: false
  },
  driver: {
    displayName: 'Driver',
    visibleTabs: ['buddy','myroute'],
    defaultTab: 'myroute',
    canApprove: false,
    seeBrainSettings: false
  },
  admin: {
    displayName: 'Admin',
    visibleTabs: ['buddy','operations','sales','pricing','marketing','finance','strategy','myroute','settings'],
    defaultTab: 'buddy',
    canApprove: true,
    seeBrainSettings: true
  }
};

const PROMPTS = {
  ops: [
    "What loads are due this week?",
    "Draft Town & Country shipment confirmation",
    "Fill Truck 4's open Friday slot",
    "What's running low in production?",
    "Schedule next Hamilton run"
  ],
  owner: [
    "Quote 1 load pellets to ZIP 13346",
    "Revenue by product line this month",
    "What's in the approval queue?",
    "Should we commit to Northeast Stove Supply?",
    "Draft a CEO update email"
  ],
  yard: [
    "What's on today's production schedule?",
    "Pellet line status",
    "What's on Truck 2's load-out list?"
  ],
  driver: [
    "First stop today",
    "BOL for Town & Country",
    "Total miles on this route"
  ],
  admin: [
    "What's in PRICING.md?",
    "Quote 1 load pellets + 1 load briquettes to ZIP 16434",
    "What integrations are pending?",
    "Show me today's demo narrative"
  ]
};

function greetingFor(role) {
  const name = currentFirstName || ROLES[role].displayName;
  if (role === 'admin') {
    return `Hi ${name}. I'm Lumber Buddy for Musser Biomass. Admin preview — switch roles via the toggle to see what each team member sees. Two tools are live: pricing calculator (with ZIP-to-ZIP freight) and send-to-CEO. Everything else is seeded demo data for now.`;
  }
  if (role === 'owner') {
    return `Morning ${name}. 2 quotes need your approval totaling $24,540. Revenue MTD is $112,400, up 22% vs last April. Northeast Stove Supply is the big decision this week — 2-load opener, $220k/year if it goes. Where do you want to start?`;
  }
  if (role === 'yard') {
    return `Morning. Pellet line running, briquette press queued for Wed. 4 loads of Forest Fuel pellets ready to ship, only 2 loads of briquettes finished — Friday's TJ Coal order needs press priority. Alpha Fiber is healthy at 3 loads.`;
  }
  if (role === 'driver') {
    return `Morning. Long-haul day: 1 load Forest Fuel pellets to Town & Country in Hamilton NY (~522 mi). BOL is loaded, signed at receiving. Yard return Wed AM, then pre-trip for the Spartansburg PA briquette run.`;
  }
  // ops
  return `Morning ${name}. 9 loads scheduled this week (5 pellets, 3 briquettes, 1 fiber), 2 quotes waiting on owner approval ($24,540 total). Briquette stock is tight — press is queued for Wed. What do you want to tackle first?`;
}

let currentRole = 'ops';
let currentFirstName = null;
let isAdmin = false;
let history = [];
let loading = false;
let lastBrainMessage = ''; // for "Send to CEO"
let priceBookCache = null; // populated from /api/quote GET

// ── URL-BASED ROLE DETECTION ──
function detectRoleFromUrl() {
  const params = new URLSearchParams(window.location.search);
  const email = (params.get('email') || '').toLowerCase();
  const name = params.get('name') || '';
  const explicitRole = (params.get('role') || '').toLowerCase();

  if (name) currentFirstName = name;

  if (explicitRole === 'admin') return 'admin';
  if (explicitRole === 'owner' || explicitRole === 'ceo') return 'owner';
  if (explicitRole === 'ops' || explicitRole === 'operations') return 'ops';
  if (explicitRole === 'yard') return 'yard';
  if (explicitRole === 'driver') return 'driver';

  if (email.includes('labsobsidian') || email.includes('@obsidianlabs') || email.includes('garrett')) {
    return 'admin';
  }
  if (email.includes('ceo') || email.includes('owner')) return 'owner';
  if (email.includes('driver')) return 'driver';
  if (email.includes('yard')) return 'yard';

  return 'ops';
}

// ── APPLY ROLE ──
function applyRole(role) {
  currentRole = role;
  const roleDef = ROLES[role];

  document.querySelectorAll('.nav-item').forEach(item => {
    const tab = item.getAttribute('data-tab');
    if (roleDef.visibleTabs.includes(tab)) {
      item.classList.remove('hidden');
    } else {
      item.classList.add('hidden');
    }
  });

  document.querySelectorAll('.approval-actions').forEach(el => {
    if (!el.getAttribute('data-original-html')) {
      el.setAttribute('data-original-html', el.innerHTML);
    }
    if (roleDef.canApprove) {
      el.innerHTML = el.getAttribute('data-original-html');
    } else {
      el.innerHTML = '<span class="owner-lock"><svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="6" width="8" height="6" rx="1"/><path d="M5 6V4a2 2 0 014 0v2"/></svg>Owner approval required</span>';
    }
  });

  const alertText = document.getElementById('sales-alert-text');
  if (alertText) {
    if (roleDef.canApprove) {
      alertText.innerHTML = '2 quotes awaiting approval — <strong style="margin-left:4px">$16,800 total.</strong>';
    } else {
      alertText.innerHTML = '2 quotes awaiting owner approval — <strong style="margin-left:4px">$16,800 total.</strong>';
    }
  }

  const brainSettings = document.getElementById('brain-settings-section');
  if (brainSettings) {
    if (roleDef.seeBrainSettings) brainSettings.classList.remove('hidden');
    else brainSettings.classList.add('hidden');
  }

  const adminBanner = document.getElementById('admin-banner');
  const userTabsEl = document.getElementById('user-tabs');
  const userLabel = document.getElementById('current-user-label');

  if (isAdmin) {
    adminBanner.classList.add('visible');
    userTabsEl.classList.remove('hidden');
    userLabel.style.display = 'none';
    const previewName = role === 'admin' ? 'Admin (all views)' : roleDef.displayName;
    document.getElementById('admin-current-role').textContent = previewName;
    document.querySelectorAll('.user-tab').forEach(t => t.classList.remove('active'));
    const btnRole = role === 'admin' ? null : role;
    if (btnRole) {
      const btn = document.querySelector(`.user-tab[data-role="${btnRole}"]`);
      if (btn) btn.classList.add('active');
    }
  } else {
    adminBanner.classList.remove('visible');
    userTabsEl.classList.add('hidden');
    userLabel.style.display = 'block';
    const displayName = currentFirstName
      ? currentFirstName.charAt(0).toUpperCase() + currentFirstName.slice(1).toLowerCase()
      : roleDef.displayName;
    userLabel.textContent = displayName;
  }

  const defaultTabBtn = document.querySelector(`.nav-item[data-tab="${roleDef.defaultTab}"]`);
  if (defaultTabBtn) showPage(roleDef.defaultTab, defaultTabBtn);

  history = [];
  lastBrainMessage = '';
  document.getElementById('chat-messages').innerHTML = '';
  buildQuickStrip(role);
  const displayName = currentFirstName && role !== 'admin'
    ? currentFirstName.charAt(0).toUpperCase() + currentFirstName.slice(1).toLowerCase()
    : roleDef.displayName;
  addMsg('brain', greetingFor(role));
}

function switchRole(newRole, btn) {
  // Admin switcher: 'ops', 'owner', 'yard', 'driver'
  applyRole(newRole);
}

function showPage(tab, el) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById('page-' + tab).classList.add('active');
  document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
  if (el) el.classList.add('active');
  // Initialize pricing calc when tab opens (idempotent)
  if (tab === 'pricing') initPricingCalc();
}

function buildQuickStrip(role) {
  const strip = document.getElementById('quick-strip');
  strip.innerHTML = '';
  const prompts = PROMPTS[role] || PROMPTS.ops;
  prompts.forEach(p => {
    const chip = document.createElement('div');
    chip.className = 'qchip';
    chip.textContent = p;
    chip.onclick = () => { if (!loading) fire(p); };
    strip.appendChild(chip);
  });
}

// ── CHAT MESSAGES ──
function addMsg(role, text, opts = {}) {
  const c = document.getElementById('chat-messages');
  const d = document.createElement('div');
  d.className = 'msg ' + (role === 'brain' ? 'brain' : 'user');
  const av = document.createElement('div');
  av.className = 'msg-av ' + (role === 'brain' ? 'brain' : 'user');
  if (role === 'brain') {
    av.innerHTML = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="8" r="3"/><circle cx="12" cy="8" r="3"/><circle cx="18" cy="8" r="3"/><circle cx="9" cy="16" r="3"/><circle cx="15" cy="16" r="3"/></svg>';
  } else {
    const letter = currentFirstName
      ? currentFirstName.charAt(0).toUpperCase()
      : (ROLES[currentRole].displayName.charAt(0));
    av.textContent = letter;
  }
  const b = document.createElement('div');
  b.className = 'msg-bubble';
  b.style.whiteSpace = 'pre-wrap';
  b.textContent = text;
  d.appendChild(av);
  d.appendChild(b);
  c.appendChild(d);

  // Add "Send to CEO" action on brain messages (except greeting)
  if (role === 'brain' && opts.withActions !== false && history.length > 0) {
    lastBrainMessage = text;
    const actions = document.createElement('div');
    actions.className = 'msg-actions';
    const btn = document.createElement('button');
    btn.className = 'msg-action-btn';
    btn.innerHTML = '✉ Send to CEO';
    btn.onclick = () => openEmailModal({
      subject: 'Lumber Buddy update · ' + new Date().toLocaleDateString(),
      body: text
    });
    actions.appendChild(btn);
    c.appendChild(actions);
  }

  c.scrollTop = c.scrollHeight;
}

function addStreamingMsg() {
  const c = document.getElementById('chat-messages');
  const d = document.createElement('div');
  d.className = 'msg brain';
  const av = document.createElement('div');
  av.className = 'msg-av brain';
  av.innerHTML = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="8" r="3"/><circle cx="12" cy="8" r="3"/><circle cx="18" cy="8" r="3"/><circle cx="9" cy="16" r="3"/><circle cx="15" cy="16" r="3"/></svg>';
  const b = document.createElement('div');
  b.className = 'msg-bubble';
  b.style.whiteSpace = 'pre-wrap';
  d.appendChild(av);
  d.appendChild(b);
  c.appendChild(d);
  c.scrollTop = c.scrollHeight;
  return { bubble: b, wrapper: d };
}

async function fire(text) { if (!loading) await run(text); }

async function sendMsg() {
  if (loading) return;
  const inp = document.getElementById('chat-input');
  const text = inp.value.trim();
  if (!text) return;
  inp.value = '';
  inp.style.height = 'auto';
  await run(text);
}

async function run(text) {
  loading = true;
  document.getElementById('send-btn').disabled = true;
  addMsg('user', text);
  history.push({ role: 'user', content: text });

  const { bubble, wrapper } = addStreamingMsg();
  let fullReply = '';

  try {
    const resp = await fetch('/api/claude', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ role: currentRole, messages: history })
    });

    if (!resp.ok) throw new Error('API error');

    const reader = resp.body.getReader();
    const decoder = new TextDecoder();

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      const chunk = decoder.decode(value, { stream: true });
      const lines = chunk.split('\n');
      for (const line of lines) {
        if (!line.startsWith('data: ')) continue;
        const data = line.slice(6).trim();
        if (data === '[DONE]' || !data) continue;
        try {
          const parsed = JSON.parse(data);
          if (parsed.type === 'content_block_delta' && parsed.delta?.type === 'text_delta') {
            fullReply += parsed.delta.text;
            bubble.textContent = fullReply;
            const c = document.getElementById('chat-messages');
            c.scrollTop = c.scrollHeight;
          }
        } catch(e) {}
      }
    }

    if (fullReply) {
      history.push({ role: 'assistant', content: fullReply });
      lastBrainMessage = fullReply;
      // Attach Send-to-CEO action to this bubble
      const actions = document.createElement('div');
      actions.className = 'msg-actions';
      const btn = document.createElement('button');
      btn.className = 'msg-action-btn';
      btn.innerHTML = '✉ Send to CEO';
      btn.onclick = () => openEmailModal({
        subject: 'Lumber Buddy update · ' + new Date().toLocaleDateString(),
        body: fullReply
      });
      actions.appendChild(btn);
      wrapper.after(actions);
      const c = document.getElementById('chat-messages');
      c.scrollTop = c.scrollHeight;
    } else {
      bubble.textContent = 'No response. Try again.';
    }

  } catch(e) {
    bubble.textContent = 'Deploy to Vercel and set env vars (ANTHROPIC_API_KEY, GITHUB_TOKEN, CLIENT_REPO, CLIENT_NAME, CLIENT_SLUG) to activate the live brain.';
  }

  loading = false;
  document.getElementById('send-btn').disabled = false;
}

function handleKey(e) {
  if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendMsg(); }
  const ta = e.target;
  ta.style.height = 'auto';
  ta.style.height = Math.min(ta.scrollHeight, 100) + 'px';
}

function approveJob(btn, amount) {
  btn.textContent = '✓ Approved';
  btn.style.background = 'var(--success-bg)';
  btn.style.color = 'var(--success)';
  btn.disabled = true;
  if (btn.nextElementSibling) btn.nextElementSibling.disabled = true;
  const badge = document.getElementById('approval-badge');
  const n = parseInt(badge.textContent) - 1;
  badge.textContent = n;
  if (n === 0) badge.style.display = 'none';
}

// ══════════════════════════════════════════════════════════════════════
// PRICING CALCULATOR (Real tool — calls /api/quote)
// ══════════════════════════════════════════════════════════════════════

let calcInitialized = false;
let calcLineCounter = 0;

async function initPricingCalc() {
  if (calcInitialized) return;
  calcInitialized = true;
  // Fetch price book from /api/quote GET
  try {
    const r = await fetch('/api/quote');
    if (r.ok) priceBookCache = await r.json();
  } catch(e) {
    // fallback: minimal hardcoded mirror so UI works even if API is down
    priceBookCache = {
      priceBook: {
        forest_fuel_pellets:    { label: 'Forest Fuel Heating Pellets', unit: 'load', unitDetail: '1,100 bags @ 40 lb', pricePerLoad: 5720.00 },
        forest_fuel_briquettes: { label: 'Forest Fuel Briquettes',      unit: 'load', unitDetail: '2,112 6-packs',     pricePerLoad: 5280.00 },
        alpha_fiber:            { label: 'Alpha Fiber',                  unit: 'load', unitDetail: '1,170 bales @ 25 lb', pricePerLoad: 6201.00 }
      }
    };
  }
  // Seed with one default line — 1 load of pellets
  addCalcLine('forest_fuel_pellets', 1);
}

function addCalcLine(presetSku, presetQty) {
  const container = document.getElementById('calc-lines');
  const id = 'calc-line-' + (++calcLineCounter);
  const row = document.createElement('div');
  row.className = 'calc-row';
  row.id = id;

  // Build SKU dropdown
  const sel = document.createElement('select');
  sel.onchange = recalcQuote;
  if (priceBookCache && priceBookCache.priceBook) {
    for (const [sku, entry] of Object.entries(priceBookCache.priceBook)) {
      const opt = document.createElement('option');
      opt.value = sku;
      const price = entry.pricePerLoad != null ? entry.pricePerLoad : entry.price;
      const detail = entry.unitDetail ? ` (${entry.unitDetail})` : '';
      opt.textContent = `${entry.label}${detail} — $${Number(price).toLocaleString('en-US', {minimumFractionDigits:2, maximumFractionDigits:2})}/${entry.unit}`;
      sel.appendChild(opt);
    }
  }
  if (presetSku) sel.value = presetSku;

  const qty = document.createElement('input');
  qty.type = 'number';
  qty.min = '1';
  qty.step = '1';
  qty.value = presetQty != null ? presetQty : '1';
  qty.oninput = recalcQuote;
  qty.onchange = recalcQuote;

  const unit = document.createElement('input');
  unit.type = 'text';
  unit.readOnly = true;
  unit.value = priceBookCache?.priceBook?.[sel.value]?.unit || '';
  unit.style.background = 'var(--bg)';
  unit.style.color = 'var(--brown-mid)';
  // keep unit in sync with sku
  sel.addEventListener('change', () => {
    unit.value = priceBookCache?.priceBook?.[sel.value]?.unit || '';
  });

  const remove = document.createElement('button');
  remove.className = 'remove-btn';
  remove.textContent = '×';
  remove.onclick = () => { row.remove(); recalcQuote(); };

  row.appendChild(sel);
  row.appendChild(qty);
  row.appendChild(unit);
  row.appendChild(remove);
  container.appendChild(row);
  recalcQuote();
}

async function recalcQuote() {
  const lines = Array.from(document.querySelectorAll('#calc-lines .calc-row')).map(row => {
    const sel = row.querySelector('select');
    const qty = row.querySelector('input[type="number"]');
    return { sku: sel.value, quantity: parseFloat(qty.value) || 0 };
  }).filter(l => l.sku && l.quantity > 0);

  const originZip = (document.getElementById('calc-from-zip').value || '24368').trim();
  const destinationZip = (document.getElementById('calc-to-zip').value || '').trim();
  const customerType = document.getElementById('calc-customer-type').value;
  const distEl = document.getElementById('calc-distance-info');

  if (lines.length === 0) {
    renderQuoteSummary({ subtotal: 0, delivery: { total: 0 }, total: 0, lines: [] });
    if (distEl) distEl.style.display = 'none';
    return;
  }

  // Only send the ZIPs to /api/quote when destination is a complete 5-digit ZIP.
  // Until then, the user is mid-typing and we don't want a flurry of bad requests.
  const body = { items: lines, customerType };
  if (/^\d{5}$/.test(destinationZip)) {
    body.originZip = originZip;
    body.destinationZip = destinationZip;
  }

  try {
    const resp = await fetch('/api/quote', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body)
    });
    if (!resp.ok) throw new Error('quote endpoint error');
    const data = await resp.json();
    renderQuoteSummary(data);
    renderDistanceInfo(data.distanceInfo);
  } catch(e) {
    document.getElementById('qs-grand-total').textContent = '—';
    document.getElementById('qs-breakdown').innerHTML =
      '<div class="qs-line" style="color:var(--red)">Quote endpoint not reachable. Deploy /api/quote to activate.</div>';
  }
}

function renderDistanceInfo(info) {
  const el = document.getElementById('calc-distance-info');
  if (!el) return;
  if (!info) { el.style.display = 'none'; el.innerHTML = ''; return; }
  if (info.error) {
    el.style.display = 'block';
    el.style.color = 'var(--red)';
    el.innerHTML = `Couldn't look up that ZIP — ${info.error}`;
    return;
  }
  el.style.display = 'block';
  el.style.color = 'var(--brown-mid)';
  el.innerHTML = `${info.from.place} → ${info.to.place} · est. <strong>${info.estimatedDrivingMiles} driving miles</strong> <span style="color:var(--brown-light)">(great-circle ${info.greatCircleMiles} mi × 1.20)</span>`;
}

function renderQuoteSummary(quote) {
  document.getElementById('qs-grand-total').textContent = '$' + (quote.total ?? 0).toFixed(2);
  const bd = document.getElementById('qs-breakdown');
  let html = '';
  (quote.lines || []).forEach(l => {
    if (l.error) {
      html += `<div class="qs-line" style="color:var(--red)"><span>${l.label}</span><span>—</span></div>`;
      return;
    }
    const qtyStr = `${l.quantity} ${l.unit}`;
    const effStr = l.effectiveUnitPrice != null && l.effectiveUnitPrice !== l.unitPrice
      ? ` @ $${l.effectiveUnitPrice.toFixed(2)}`
      : '';
    html += `<div class="qs-line"><span>${l.label} · ${qtyStr}${effStr}</span><span>$${(l.extendedPrice || 0).toFixed(2)}</span></div>`;
  });
  html += `<div class="qs-line"><span>Subtotal</span><span>$${(quote.subtotal || 0).toFixed(2)}</span></div>`;
  if (quote.delivery) {
    const dnote = quote.delivery.note || '';
    html += `<div class="qs-line"><span>Freight${dnote ? ' · ' + dnote : ''}</span><span>$${(quote.delivery.total || 0).toFixed(2)}</span></div>`;
  }
  html += `<div class="qs-line strong"><span>Grand total</span><span>$${(quote.total || 0).toFixed(2)}</span></div>`;
  bd.innerHTML = html;
}

// ══════════════════════════════════════════════════════════════════════
// SEND TO CEO (Real tool — calls /api/send-email)
// ══════════════════════════════════════════════════════════════════════

function openEmailModal({ subject = '', body = '' } = {}) {
  document.getElementById('email-subject').value = subject;
  document.getElementById('email-body').value = body;
  const status = document.getElementById('email-status');
  status.className = 'modal-status';
  status.textContent = '';
  document.getElementById('email-modal').classList.add('visible');
}

function closeEmailModal() {
  document.getElementById('email-modal').classList.remove('visible');
}

async function sendEmailToCeo() {
  const subject = document.getElementById('email-subject').value.trim();
  const body = document.getElementById('email-body').value.trim();
  const status = document.getElementById('email-status');
  const btn = document.getElementById('email-send-btn');

  if (!subject || !body) {
    status.className = 'modal-status error';
    status.textContent = 'Subject and body are both required.';
    return;
  }

  btn.disabled = true;
  status.className = 'modal-status sending';
  status.textContent = 'Sending via Resend…';

  try {
    const resp = await fetch('/api/send-email', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ preset: 'to_ceo', subject, body })
    });
    const data = await resp.json();
    if (!resp.ok) {
      status.className = 'modal-status error';
      status.textContent = `Send failed: ${data.error || 'unknown error'}${data.detail ? ' · ' + (typeof data.detail === 'string' ? data.detail : JSON.stringify(data.detail)) : ''}`;
      btn.disabled = false;
      return;
    }
    status.className = 'modal-status success';
    status.textContent = `✓ Sent to ${data.sentTo} · Resend ID: ${data.resendId || 'n/a'}`;
    setTimeout(() => { closeEmailModal(); btn.disabled = false; }, 1800);
  } catch(err) {
    status.className = 'modal-status error';
    status.textContent = 'Network error: ' + err.message;
    btn.disabled = false;
  }
}

// Close modal on overlay click
document.getElementById('email-modal').addEventListener('click', (e) => {
  if (e.target.id === 'email-modal') closeEmailModal();
});

// ══════════════════════════════════════════════════════════════════════
// INIT
// ══════════════════════════════════════════════════════════════════════

window.addEventListener('load', () => {
  const detectedRole = detectRoleFromUrl();
  isAdmin = (detectedRole === 'admin');
  applyRole(detectedRole);
});
</script>
</body>
</html>
